# retail-sales-etl-analytics
An end-to-end ETL and analytics pipeline that extracts, validates, transforms, and loads retail sales data into PostgreSQL, with SQL-based KPI analysis and interactive Power BI dashboards.
